pool:
  vmImage: "ubuntu-latest"

resources:
  pipelines:
  - pipeline: adoBuild
    source: ado-build
    trigger: 
      branches:
        include:
          - 'main'
          - 'release/*'

steps:
- script: |
    echo HERE2
