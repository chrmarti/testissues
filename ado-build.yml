pool:
  vmImage: "ubuntu-latest"

trigger:
  branches:
    include:
      - 'main'
      - 'release/*'
pr: none

stages:
  - stage: Compile
    jobs:
      - job: Compile
        # pool: vscode-1es-linux
        steps:
          - script: |
              echo HERE
              sleep 10
              echo HERE2
              # exit 1
  - stage: IM Notification
    dependsOn:
      - Compile
    # pool: vscode-1es-linux
    jobs:
      - job: IM Notification
        steps:
          - template: ado-build-chat.yml
