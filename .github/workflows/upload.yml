name: Test Upload

on:
  push:
    branches:
      - '**'

jobs:
  stuff:
    runs-on: ubuntu-latest
    steps:
    - run: echo hello > world.txt
    - uses: actions/upload-artifact@v3
      with:
        name: my-artifact
        path: world.txt
